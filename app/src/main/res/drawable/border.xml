<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Borde inferior -->
    <item
        android:top="-2dp"
        android:right="-2dp"
        android:left="-2dp">
        <shape>
            <solid android:color="@android:color/transparent"/> <!-- color del borde cambiado a transparente -->
            <stroke android:color="#302121" android:width="2dp"/> <!-- color y grosor del borde -->
        </shape>
    </item>
</layer-list>